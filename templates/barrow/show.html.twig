{% extends 'base.html.twig' %}

{% block title %}Barrow{% endblock %}

{% block body %}
    <h1>Barrow</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ barrow.id }}</td>
            </tr>
            <tr>
                <th>BarrowDate</th>
                <td>{{ barrow.barrowDate ? barrow.barrowDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>DueDate</th>
                <td>{{ barrow.dueDate ? barrow.dueDate|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('barrow_index') }}">back to list</a>

    <a href="{{ path('barrow_edit', {'id': barrow.id}) }}">edit</a>

    {{ include('barrow/_delete_form.html.twig') }}
{% endblock %}
