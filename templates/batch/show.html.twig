{% extends 'base.html.twig' %}

{% block title %}Batch{% endblock %}

{% block body %}
    <h1>Batch</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ batch.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ batch.name }}</td>
            </tr>
            <tr>
                <th>Catogary</th>
                <td>{{ batch.catogary }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ batch.description }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ batch.price }}</td>
            </tr>
            <tr>
                <th>NoOfBook</th>
                <td>{{ batch.noOfBook }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('batch_index') }}">back to list</a>

    <a href="{{ path('batch_edit', {'id': batch.id}) }}">edit</a>

    {{ include('batch/_delete_form.html.twig') }}
{% endblock %}
